# SliderImages.vue

**Path**: `apps\front-ecommerce\components\content\SliderImages.vue`

## Summary
This Vue component displays a responsive image slider/carousel for an e-commerce storefront. On mobile devices, it renders images in a swipeable carousel format, while on desktop (≥1024px) it displays images in a static 3-column grid layout. The component handles Strapi CMS image data with optional overlay content and uses different image sizes optimized for various breakpoints.

