# usePrice.ts

**Path**: `apps\front-ecommerce\composables\usePrice.ts`

## Summary
This composable formats monetary amounts for display in an e-commerce application, converting values from cents to currency format. It handles special cases including zero amounts (displaying as "free"), invalid amounts (displaying as placeholder dashes), and optional numeric-only output, using Vue's i18n for localized currency formatting.

