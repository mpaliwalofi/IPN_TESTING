# frequency.ts

**Path**: `apps\front-ecommerce\types\ecommerce\frequency.ts`

## Summary
This TypeScript file defines the `IFrequency` interface for managing subscription or recurring order frequency options in an e-commerce application. The interface specifies properties for identifying frequency types (code, label, description), their duration in days, and display ordering (position), which likely supports features like "weekly", "bi-weekly", or "monthly" subscription deliveries.

## Interfaces
- `IFrequency`

