# akeneo.js

**Path**: `config\akeneo.js`

## Summary
This configuration file exports Akeneo PIM (Product Information Management) integration settings for a Strapi application. It centralizes all necessary credentials and parameters (API keys, secrets, endpoint, username/password) required to authenticate and synchronize product data with Akeneo, including specialized secrets for localization and translation services.

