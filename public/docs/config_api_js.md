# api.js

**Path**: `config\api.js`

## Summary
This configuration file defines default REST API pagination and response settings for the Strapi application. It sets the default page size to 25 items, enforces a maximum limit of 100 items per request, and enables count metadata in API responses. These settings help standardize API behavior and prevent performance issues from excessive data retrieval.

