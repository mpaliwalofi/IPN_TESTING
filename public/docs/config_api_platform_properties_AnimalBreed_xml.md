# AnimalBreed.xml

**Path**: `config\api_platform\properties\AnimalBreed.xml`

## Summary
This XML configuration file defines API Platform property metadata for the AnimalBreed resource. It specifies that the `id` field serves as the non-writable identifier and the `code` field is also read-only, controlling how these properties are exposed and handled in the API for managing animal breed data.

