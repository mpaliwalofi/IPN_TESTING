# AnimalGender.xml

**Path**: `config\api_platform\properties\AnimalGender.xml`

## Summary
This API Platform configuration file defines metadata for the `AnimalGender` resource property. It specifies that the `id` field serves as the resource identifier and is read-only (not writable), establishing how the AnimalGender entity's primary key should be handled in API operations.

