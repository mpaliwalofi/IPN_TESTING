# EmailAudit.xml

**Path**: `config\api_platform\properties\EmailAudit.xml`

## Summary
This API Platform configuration file defines metadata properties for an EmailAudit resource entity. It specifies that the `id` field serves as the non-writable identifier for the EmailAudit resource, which is used for tracking and auditing email-related activities in the system's REST API.

