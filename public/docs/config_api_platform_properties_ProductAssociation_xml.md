# ProductAssociation.xml

**Path**: `config\api_platform\properties\ProductAssociation.xml`

## Summary
This API Platform configuration file defines metadata for the ProductAssociation entity in a Sylius e-commerce application. It specifies that the `id` property serves as the resource identifier and is read-only (not writable), establishing how product associations are exposed and handled through the API.

