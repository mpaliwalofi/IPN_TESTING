# ShippingMethodRule.xml

**Path**: `config\api_platform\properties\ShippingMethodRule.xml`

## Summary
This API Platform configuration file defines metadata properties for the ShippingMethodRule entity in a Sylius e-commerce system. It specifies that the `id` property serves as the resource identifier and is read-only (not writable), enabling the shipping method rule entity to be exposed through the API with proper identification constraints.

