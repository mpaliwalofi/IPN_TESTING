# SubscriptionMonitoring.xml

**Path**: `config\api_platform\properties\SubscriptionMonitoring.xml`

## Summary
This API Platform configuration file defines metadata properties for a SubscriptionMonitoring resource. It specifies that the `id` property serves as the resource identifier and is read-only (not writable), which is a standard configuration for exposing subscription monitoring entities through the API Platform framework.

