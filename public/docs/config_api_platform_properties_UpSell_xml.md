# UpSell.xml

**Path**: `config\api_platform\properties\UpSell.xml`

## Summary
This API Platform configuration file defines metadata properties for an UpSell resource entity. It specifies that the "id" property serves as the resource identifier and is read-only (not writable), establishing the primary key configuration for UpSell API endpoints used in cross-selling or product recommendation functionality.

