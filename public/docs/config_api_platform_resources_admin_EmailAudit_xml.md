# EmailAudit.xml

**Path**: `config\api_platform\resources\admin\EmailAudit.xml`

## Summary
This API Platform configuration file defines admin-level REST API endpoints for the EmailAudit resource. It exposes two read-only operations: retrieving a collection of email audit records and fetching individual email audit entries, both accessible under the `/admin` route prefix for administrative monitoring and review of email activity.

