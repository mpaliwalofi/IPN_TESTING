# AnimalKibbleBrandRange.xml

**Path**: `config\api_platform\resources\shop\AnimalKibbleBrandRange.xml`

## Summary
This API Platform configuration file defines a REST API resource for accessing animal kibble brand ranges in a shop context, likely part of a Sylius e-commerce application for pet supplies. It configures a single GET endpoint (`/kibble_brand_ranges/{id}`) that retrieves individual brand range details with read-only permissions, serializing the response using the `shop:animal_kibble_brand_range:read` normalization group.

