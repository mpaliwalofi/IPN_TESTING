# database.js

**Path**: `config\database.js`

## Summary
This configuration file exports database connection settings for a Strapi CMS application using MySQL as the database backend. It defines connection parameters (host, port, credentials, SSL) with environment variable overrides and defaults, enabling the application to connect to a MySQL database named "upd-cms" with UTF-8 character encoding and UTC timezone settings.

