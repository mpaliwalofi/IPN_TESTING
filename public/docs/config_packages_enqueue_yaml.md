# enqueue.yaml

**Path**: `config\packages\enqueue.yaml`

## Summary
This is a configuration file for the Enqueue messaging library in a Symfony application. It sets up a default message transport using the "gps:" DSN (Data Source Name), which likely refers to Google Pub/Sub as the message broker for asynchronous message processing and job queuing.

