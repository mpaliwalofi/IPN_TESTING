# paramters.yaml

**Path**: `config\packages\prod\paramters.yaml`

## Summary
This Symfony configuration file defines container-specific parameters optimized for the production environment. It enables inline factory optimization for better performance and disables container dumping for debugging purposes, reducing overhead and preventing exposure of internal container structure in production deployments.

