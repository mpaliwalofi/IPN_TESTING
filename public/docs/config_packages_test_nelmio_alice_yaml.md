# nelmio_alice.yaml

**Path**: `config\packages\test\nelmio_alice.yaml`

## Summary
This configuration file imports the development environment settings for Nelmio Alice (a Symfony bundle for generating test fixtures) into the test environment. It ensures that the test environment inherits all fixture loading configurations defined in the dev environment, promoting consistency between development and testing fixture data generation.

