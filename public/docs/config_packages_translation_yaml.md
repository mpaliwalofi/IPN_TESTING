# translation.yaml

**Path**: `config\packages\translation.yaml`

## Summary
This Symfony framework configuration file sets up the translation system for internationalization (i18n) support. It defines the default locale using a parameter, configures fallback languages for missing translations, and specifies the directory path where translation files are stored. This enables the application to serve content in multiple languages by loading appropriate translation resources from the translations folder.

