# validator.yaml

**Path**: `config\packages\validator.yaml`

## Summary
This Symfony framework configuration file enables and configures the validation component. It activates attribute-based validation (allowing validation rules to be defined via PHP attributes) and sets the email validation mode to 'html5' standard, which uses a simpler regex pattern compatible with HTML5 email input validation rather than strict RFC compliance.

