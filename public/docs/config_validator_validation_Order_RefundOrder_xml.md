# RefundOrder.xml

**Path**: `config\validator\validation\Order\RefundOrder.xml`

## Summary
This XML configuration file defines validation constraints for the `RefundOrder` command in an order management system. It applies a custom `OrderRefundCompletion` constraint to validate refund operations, specifically within the `app_refund_order` validation group, ensuring that order refund requests meet business rules before processing.

