# types.ts

**Path**: `packages\accordionFold\src\types.ts`

## Summary
This TypeScript file defines type definitions for the accordionFold package. Currently, it exports a single generic `Options` type aliased to `object`, which serves as a placeholder for configuration options that can be passed to the accordion fold functionality. This type will likely be extended with specific properties to configure accordion folding behavior such as animation duration, collapse settings, or transition effects.

## Type Aliases
- `Options`

