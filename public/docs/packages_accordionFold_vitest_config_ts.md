# vitest.config.ts

**Path**: `packages\accordionFold\vitest.config.ts`

## Summary
This configuration file sets up Vitest (a testing framework) specifically for the `accordionFold` package within a monorepo structure. It inherits all testing configurations from a shared base configuration file located at the repository root, without adding any package-specific test settings or overrides.

