# project.json

**Path**: `packages\boilerplate\project.json`

## Summary
This is an Nx workspace configuration file for a library package named "boilerplate". It defines the project structure (source root at `packages/boilerplate/src`) and configures available build targets, specifically a linting task using ESLint with auto-fix enabled. This configuration enables Nx to manage and orchestrate commands for this boilerplate library within the monorepo.

