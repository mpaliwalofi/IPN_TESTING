# constants.ts

**Path**: `packages\boilerplate\src\constants.ts`

## Summary
This file defines a Vue 3 injection key constant used for dependency injection within the ultrapremiumdirect design system boilerplate. The `InjectionKey` symbol serves as a type-safe identifier for providing and injecting `Options` configuration throughout the Vue component hierarchy, enabling components to access shared design system settings without prop drilling.

