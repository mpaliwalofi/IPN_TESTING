# vitest.config.ts

**Path**: `packages\boilerplate\vitest.config.ts`

## Summary
This is a Vitest configuration file for the boilerplate package that extends a base Vitest configuration from the monorepo root. It merges the shared base configuration without adding any package-specific test settings, enabling consistent test behavior across the monorepo while allowing for future customization if needed.

