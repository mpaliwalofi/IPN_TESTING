# package.json

**Path**: `packages\carousel\package.json`

## Summary
This package.json defines a Vue 3-based carousel component library for the UltraPremiumDirect design system. It configures the build process using Vite to generate ES module distributions, exposes both the main carousel component and a Tailwind CSS plugin, and includes necessary dependencies for Vue composition with VueUse utilities and internationalization support.

