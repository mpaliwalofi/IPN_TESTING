# constants.ts

**Path**: `packages\carousel\src\constants.ts`

## Summary
This file defines a Vue injection key for the carousel component's dependency injection system. It creates a type-safe symbol used to provide and inject carousel configuration options throughout the component hierarchy, ensuring proper type checking when sharing carousel settings between parent and child components.

