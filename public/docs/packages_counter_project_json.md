# project.json

**Path**: `packages\counter\project.json`

## Summary
This is an Nx workspace configuration file for a library package called "counter". It defines the project structure, marking it as a library type with source code in `packages/counter/src`, and configures a single linting target using ESLint with auto-fix enabled.

