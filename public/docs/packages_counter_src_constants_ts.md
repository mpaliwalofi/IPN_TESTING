# constants.ts

**Path**: `packages\counter\src\constants.ts`

## Summary
This file defines a Vue injection key for a counter component within a design system package. The injection key enables type-safe dependency injection of counter options throughout the Vue component hierarchy, allowing parent components to provide counter configuration that child components can consume without prop drilling.

