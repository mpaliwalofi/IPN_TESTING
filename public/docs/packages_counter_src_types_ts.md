# types.ts

**Path**: `packages\counter\src\types.ts`

## Summary
This TypeScript file defines type definitions for a counter package, currently containing only an empty `Options` interface. It serves as a centralized location for type declarations that can be imported and used throughout the counter package to provide type safety and documentation for configuration options.

## Interfaces
- `Options`

