# vitest.config.ts

**Path**: `packages\counter\vitest.config.ts`

## Summary
This is a Vitest configuration file for the `counter` package that extends a base testing configuration. It merges the workspace-level base Vitest config without adding any package-specific test configuration overrides, establishing a standard testing setup for the counter package's unit tests.

