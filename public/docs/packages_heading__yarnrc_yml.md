# .yarnrc.yml

**Path**: `packages\heading\.yarnrc.yml`

## Summary
This is a Yarn package manager configuration file that specifies the node module linking strategy. It configures Yarn to use the traditional `node-modules` linker, which creates a standard `node_modules` directory structure for dependency management, rather than using alternative linking strategies like Plug'n'Play (PnP).

