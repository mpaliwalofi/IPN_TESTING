# project.json

**Path**: `packages\heading\project.json`

## Summary
This is an Nx project configuration file for a "heading" library package within a design system (indicated by the "upd:ds" tag). It defines the project structure, marks it as a library component, and configures basic linting capabilities using ESLint with auto-fix enabled.

