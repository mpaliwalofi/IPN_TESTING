# constants.ts

**Path**: `packages\heading\src\constants.ts`

## Summary
This file defines a Vue injection key constant used for providing and injecting heading component options throughout the component hierarchy. It establishes a type-safe dependency injection mechanism specifically for the ultrapremiumdirect design system's heading component, allowing parent components to pass configuration options down to child heading components without prop drilling.

