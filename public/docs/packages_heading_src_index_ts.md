# index.ts

**Path**: `packages\heading\src\index.ts`

## Summary
This file serves as the main entry point for a Vue 3 heading component plugin within a design system package. It exports a `Heading` component and provides a Vue plugin (`UltraPremiumDirectDesignSystemHeadingPlugin`) that registers the component globally and optionally accepts configuration options via Vue's dependency injection system. The plugin enables consistent heading styling and behavior across an application by centralizing the component registration and configuration.

