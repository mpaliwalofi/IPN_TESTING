# project.json

**Path**: `packages\separator\project.json`

## Summary
This is an Nx project configuration file for a library package called "separator". It defines the project structure, marking it as a library type with source code in `packages/separator/src`, and configures a single build target for linting using ESLint with auto-fix enabled.

