# vite.config.ts

**Path**: `packages\separator\vite.config.ts`

## Summary
This is a Vite build configuration file for the `separator` package within the UltraPremiumDirect Design System (DS). It configures the package to be built as an ES module library with the entry point at `src/index.ts`, merging with a base Vite configuration while specifying the output format and naming convention for the compiled separator component.

