# vitest.config.ts

**Path**: `packages\separator\vitest.config.ts`

## Summary
This is a Vitest configuration file for the separator package that extends a base Vitest configuration used across the monorepo. It merges the shared base configuration without adding any package-specific test settings, enabling consistent test execution behavior for the separator component's test suite.

