# types.ts

**Path**: `packages\smartForm\src\types.ts`

## Summary
This TypeScript file defines the core type definitions for the smartForm package. It currently exports a single empty `Options` interface that serves as a placeholder or base type for configuration options that will be used throughout the smartForm implementation. This is a foundational types file that establishes the type system contract for the package's API.

## Interfaces
- `Options`

