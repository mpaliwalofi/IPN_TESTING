# vitest.config.ts

**Path**: `packages\smartForm\vitest.config.ts`

## Summary
This is a Vitest configuration file for the smartForm package that extends the base testing configuration from the monorepo root. It merges the shared base configuration without adding any package-specific test settings, ensuring consistent test behavior across the monorepo while allowing for future customization if needed.

