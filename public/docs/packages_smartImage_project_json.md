# project.json

**Path**: `packages\smartImage\project.json`

## Summary
This is an Nx project configuration file for a library package called "smartImage". It defines the project structure, marking it as a library type with source code in `packages/smartImage/src`, and configures a single build target for linting using ESLint with auto-fix enabled.

