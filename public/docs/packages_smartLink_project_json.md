# project.json

**Path**: `packages\smartLink\project.json`

## Summary
This is an Nx workspace configuration file for the "smartLink" library package. It defines the project structure, designating it as a library type with source code in `packages/smartLink/src`, and configures a linting target using ESLint with automatic fix enabled. The configuration establishes the project's metadata and build tooling within the monorepo structure.

