# constants.ts

**Path**: `packages\smartLink\src\constants.ts`

## Summary
This file defines a Vue injection key for the SmartLink component's dependency injection system. It creates a type-safe symbol used to provide and inject SmartLink configuration options throughout the Vue component hierarchy, specifically for the ultrapremiumdirect design system's smart link functionality.

