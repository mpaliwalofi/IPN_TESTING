# types.ts

**Path**: `packages\smartLink\src\types.ts`

## Summary
This TypeScript file defines type interfaces for a smart link component in a Nuxt.js application. It establishes the contract for an `Options` interface that accepts a function to define custom NuxtLink components with specific link options and properties. The file serves as a type definition module to ensure type safety when creating configurable, reusable link components within the Nuxt ecosystem.

## Interfaces
- `Options`

