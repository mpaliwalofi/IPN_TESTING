# vitest.config.ts

**Path**: `packages\smartLink\vitest.config.ts`

## Summary
This is a Vitest configuration file for the `smartLink` package that sets up its testing environment. It imports and merges a base Vitest configuration from the repository root without adding any package-specific test configuration overrides, ensuring consistent test settings across the monorepo while allowing for potential future customization.

