# direction.ts

**Path**: `packages\types\src\core\direction.ts`

## Summary
This file defines a type-safe enumeration for directional values used throughout the application. It exports a constant object `DIRECTION` containing five directional string literals (left, right, center, up, down) and derives a TypeScript type `TDirection` from those values, ensuring type safety when referencing directions in the codebase.

## Type Aliases
- `TDirection`

