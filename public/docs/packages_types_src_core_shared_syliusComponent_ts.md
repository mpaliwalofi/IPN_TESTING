# syliusComponent.ts

**Path**: `packages\types\src\core\shared\syliusComponent.ts`

## Summary
This TypeScript file defines a base interface for Sylius e-commerce platform components using JSON-LD/Hydra conventions. The `ISyliusComponent` interface establishes common fields (`@id`, `@type`, `@context`) that enable linked data representation and API resource identification across the Sylius system. It serves as a foundational type that other Sylius-related interfaces likely extend to maintain consistent structured data formatting.

## Interfaces
- `ISyliusComponent`

