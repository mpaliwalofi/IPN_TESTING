# tsconfig.json

**Path**: `packages\verbatimFullWidth\tsconfig.json`

## Summary
This is a TypeScript configuration file for the "verbatimFullWidth" package that extends a base TypeScript configuration. It specifies compilation settings including file inclusion patterns for Vue components, TypeScript files, and Tailwind CSS files, while enabling JSON module resolution and adding type definitions for Vitest testing framework and Jest DOM matchers. The configuration sets up the TypeScript compiler to properly handle Vue single-file components and testing utilities within this specific package workspace.

