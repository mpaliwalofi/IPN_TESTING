# vitest.config.ts

**Path**: `packages\verbatimFullWidth\vitest.config.ts`

## Summary
This configuration file sets up Vitest (a testing framework) for the `verbatimFullWidth` package by extending a shared base configuration from the monorepo root. It currently applies no package-specific test configuration overrides, inheriting all settings from the base config. This allows the package to maintain consistent testing behavior across the monorepo while providing a location for future package-specific test customizations if needed.

