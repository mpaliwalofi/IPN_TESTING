# project.json

**Path**: `packages\youtube\project.json`

## Summary
This is an Nx workspace project configuration file for a YouTube library package. It defines the package as a library project type with source code in `packages/youtube/src`, includes a lint target using ESLint with auto-fix enabled, and is tagged with "upd:ds" for workspace organization and dependency management purposes.

