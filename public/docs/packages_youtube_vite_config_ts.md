# vite.config.ts

**Path**: `packages\youtube\vite.config.ts`

## Summary
This Vite configuration file defines the build setup for a YouTube-related package within the UltraPremiumDirect design system. It merges a base Vite configuration with package-specific settings to build a library module that exports as an ES module format with the output filename `upd-ds-youtube.es.js`. The configuration specifically targets the `src/index.ts` entry point to create a distributable library component for YouTube functionality.

