# ReplaceRequest.php

**Path**: `src\Api\Command\ProductVariant\ReplaceRequest.php`

## Summary
This class represents a Data Transfer Object (DTO) for a product variant replacement operation in an e-commerce or product management system. It encapsulates the request parameters needed to replace one product variant with another, storing the source variant code (the variant being replaced) and the target variant code (the replacement variant). This command object is likely used in a CQRS architecture to initiate a product variant swap or substitution workflow.

## Classes
- `ReplaceRequest`

