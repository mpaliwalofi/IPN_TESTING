# SubscriptionState.php

**Path**: `src\Enum\Subscription\SubscriptionState.php`

## Summary
This PHP enum defines the possible states of a subscription (ACTIVE, ERROR, PAUSED, STOPPED) in a subscription management system. It provides convenient helper methods (`isActive()`, `isOnError()`, `isPaused()`, `isStopped()`) to check the current state of a subscription, enabling type-safe state comparison throughout the application. This enum serves as a domain model component for tracking and validating subscription lifecycle states.

## Function Details

### `isActive`


### `isOnError`


### `isPaused`


### `isStopped`


