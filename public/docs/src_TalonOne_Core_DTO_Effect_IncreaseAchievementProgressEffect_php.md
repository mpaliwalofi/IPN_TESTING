# IncreaseAchievementProgressEffect.php

**Path**: `src\TalonOne\Core\DTO\Effect\IncreaseAchievementProgressEffect.php`

## Summary
This class represents a specific type of effect in the TalonOne integration that handles increasing progress towards an achievement. It extends a base `Effect` class and contains properties specific to achievement progress updates through the `IncreaseAchievementProgressEffectProps` object, which likely defines the achievement identifier and progress increment amount. This is part of TalonOne's promotion engine system where achievements are tracked and rewarded as part of customer loyalty or gamification campaigns.

## Classes
- `IncreaseAchievementProgressEffect`

