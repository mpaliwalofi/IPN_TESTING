# ValidRoles.php

**Path**: `src\Validator\Constraints\ValidRoles.php`

## Summary
This is a Symfony validation constraint class that defines a custom validation rule for role validation. It serves as a marker constraint (using PHP 8 attributes) that can be applied to properties or classes to ensure role values are valid according to the application's business rules, displaying "Invalid roles" when validation fails.

## Classes
- `ValidRoles`

