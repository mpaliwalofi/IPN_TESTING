# RemoveCartEligibility.php

**Path**: `src\Validator\RemoveCartEligibility.php`

## Summary
This class defines a Symfony validation constraint for determining eligibility to remove items from a shopping cart. It's configured as a class-level constraint (rather than property-level), meaning it validates the entire object state to enforce business rules around cart item removal operations.

## Classes
- `RemoveCartEligibility`

## Function Details

### `getTargets`


