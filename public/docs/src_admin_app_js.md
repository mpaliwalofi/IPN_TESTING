# app.js

**Path**: `src\admin\app.js`

## Summary
This is a Strapi admin panel configuration file that sets up French localization and bootstraps custom components for the content manager. Its main purpose is to inject a ThemeColorSync component into the edit view that automatically synchronizes color fields based on theme selection across different content types, enhancing the admin UI's content editing workflow.

