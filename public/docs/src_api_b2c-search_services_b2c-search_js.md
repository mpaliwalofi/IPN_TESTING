# b2c-search.js

**Path**: `src\api\b2c-search\services\b2c-search.js`

## Summary
This file defines a Strapi service for the B2C (Business-to-Consumer) search functionality. It creates a standard Strapi core service using the factory pattern, which provides default CRUD operations and database interactions for the b2c-search content type. The service acts as the business logic layer between the API controllers and the database, enabling search-related operations for B2C use cases within the Strapi CMS.

