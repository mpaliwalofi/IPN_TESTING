# email-layout.js

**Path**: `src\api\email-layout\routes\email-layout.js`

## Summary
This file defines the REST API router for the email-layout content type in a Strapi CMS application. It uses Strapi's factory function to automatically generate standard CRUD endpoints (create, read, update, delete) for managing email layout templates that are likely used for transactional or marketing email formatting across the application.

