# email-sender.js

**Path**: `src\api\email-sender\routes\email-sender.js`

## Summary
This code defines a REST API router for the email-sender content type in a Strapi application. It uses Strapi's factory function to automatically generate standard CRUD endpoints (create, read, update, delete) for managing email sender configurations or records without requiring custom route definitions.

