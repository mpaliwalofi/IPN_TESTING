# addresses-list.json

**Path**: `src\components\b2c\addresses-list.json`

## Summary
This JSON file defines a Strapi component schema for managing a B2C addresses list feature. It establishes a reusable component named "AddressesList" with a single boolean attribute "enabled" (defaulting to true) that likely controls whether the addresses list functionality is active or visible in the B2C interface.

