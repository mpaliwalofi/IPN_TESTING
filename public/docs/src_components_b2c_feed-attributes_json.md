# feed-attributes.json

**Path**: `src\components\b2c\feed-attributes.json`

## Summary
This JSON file defines a Strapi component schema for B2C feed configuration attributes. It specifies two boolean feature flags: `enable_usp` (likely Unique Selling Proposition) and `enable_tabs`, both defaulting to false, which control the display behavior of feed elements in a B2C (business-to-consumer) context.

