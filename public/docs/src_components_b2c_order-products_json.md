# order-products.json

**Path**: `src\components\b2c\order-products.json`

## Summary
This is a Strapi CMS component schema definition for B2C order products functionality. It defines a reusable component named "Order Products" with a single boolean attribute `enabled` (defaulting to true) that likely controls whether products can be ordered in a B2C context. This component serves as a data model configuration for managing product ordering capabilities within the Strapi content management system.

