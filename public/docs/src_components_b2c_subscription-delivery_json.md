# subscription-delivery.json

**Path**: `src\components\b2c\subscription-delivery.json`

## Summary
This JSON file defines a Strapi component schema for managing subscription delivery settings in a B2C (business-to-consumer) context. It creates a reusable component called "Subscription Delivery" with a single boolean attribute that controls whether subscription deliveries are enabled or disabled, defaulting to enabled state.

