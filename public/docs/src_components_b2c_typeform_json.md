# typeform.json

**Path**: `src\components\b2c\typeform.json`

## Summary
This is a Strapi CMS component schema definition for embedding Typeform surveys in a B2C (business-to-consumer) context. It defines a reusable content component that stores a single attribute (`form_id`) to reference and integrate external Typeform forms into the CMS-managed content.

