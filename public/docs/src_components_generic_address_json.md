# address.json

**Path**: `src\components\generic\address.json`

## Summary
This is a Strapi CMS component schema definition that defines a reusable address data structure. It specifies five standard address fields (street address, locality/city, postal code, country, and region/state) following common address formatting conventions, likely used across multiple content types in the application for storing location information in a consistent format.

