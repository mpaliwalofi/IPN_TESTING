# breadcrumb-item.json

**Path**: `src\components\generic\breadcrumb-item.json`

## Summary
This is a Strapi CMS component schema definition for a breadcrumb navigation item. It defines a reusable content structure with two attributes: `text` (the display label) and `url` (the navigation link), allowing content editors to build breadcrumb trails for website navigation hierarchy.

