# indexes.json

**Path**: `src\components\generic\indexes.json`

## Summary
This JSON file defines a Strapi component schema for managing generic index values. It creates a reusable "Indexes" component with a single string attribute called "index", likely used to store positional or ordering information that can be attached to other content types within the CMS.

