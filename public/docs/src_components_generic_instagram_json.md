# instagram.json

**Path**: `src\components\generic\instagram.json`

## Summary
This JSON file defines a Strapi CMS component schema for embedding Instagram content. It specifies a reusable content type with two attributes: an `embed_code` text field for storing Instagram embed HTML/code, and a `background` boolean flag (defaulting to false) likely used to control display styling or positioning of the Instagram embed within the application.

