# animal-deletion.json

**Path**: `src\components\mobile\animal-deletion.json`

## Summary
This JSON file defines a Strapi component schema for managing animal deletion functionality in a mobile application context. It configures a simple boolean flag ("enabled") that controls whether animal deletion features are active, with the flag defaulted to true and marked as required. This component likely serves as a configuration setting that can be attached to content types to toggle animal deletion capabilities within the mobile app interface.

