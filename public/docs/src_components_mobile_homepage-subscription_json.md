# homepage-subscription.json

**Path**: `src\components\mobile\homepage-subscription.json`

## Summary
This JSON file defines a Strapi component schema for managing homepage subscription functionality in a mobile application. It configures a simple boolean toggle (`enabled`) that controls whether the subscription feature is displayed or active on the mobile homepage, with the feature enabled by default.

