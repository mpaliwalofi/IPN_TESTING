# shop-reviews.json

**Path**: `src\components\mobile\shop-reviews.json`

## Summary
This JSON file defines a Strapi component schema for managing shop reviews functionality in a mobile application. It specifies a simple data structure with a single "enabled" boolean attribute that controls whether shop reviews are active or displayed, with the feature enabled by default.

