# package.json

**Path**: `src\plugins\custom-menu\package.json`

## Summary
This is a package.json configuration file for a Strapi CMS plugin called "custom-menu". It defines the plugin's metadata, including its name, version, description, and compatibility requirements (Node.js 14.19.1-18.x), establishing it as a Strapi plugin component that likely provides custom menu management functionality within the CMS.

