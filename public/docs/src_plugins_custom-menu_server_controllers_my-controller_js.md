# my-controller.js

**Path**: `src\plugins\custom-menu\server\controllers\my-controller.js`

## Summary
This is a Strapi plugin controller that handles HTTP requests for the 'custom-menu' plugin. It exports a single `index` action that retrieves a welcome message from the plugin's service layer and returns it in the response body, serving as a basic example or starting point for custom menu functionality.

