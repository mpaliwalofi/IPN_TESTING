# import-shop.js

**Path**: `src\plugins\import-akeneo\server\controllers\import-shop.js`

## Summary
This controller handles shop data import operations from Akeneo PIM into a Strapi application. It provides endpoints to import all shops or a specific shop by UID, manage OAuth token refresh/updates via authorization codes, and return appropriate redirect URLs when token refresh is needed during the import process.

