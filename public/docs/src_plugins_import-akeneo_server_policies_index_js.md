# index.js

**Path**: `src\plugins\import-akeneo\server\policies\index.js`

## Summary
This module serves as an index file that exports the import-akeneo plugin's server-side policies, specifically the `localizer` policy. It acts as a centralized export point for policy modules that likely handle authorization and request processing rules related to importing data from Akeneo PIM (Product Information Management) system.

