# transactionId.html.twig

**Path**: `templates\Ui\Grid\Field\transactionId.html.twig`

## Summary
This Twig template renders a transaction ID field in a grid view for payment-related data. It displays the transaction ID from payment data if available, otherwise shows a "none" label, providing a safe way to handle cases where payment transaction information might be missing or incomplete.

