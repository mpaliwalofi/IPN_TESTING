# export.fr.yaml

**Path**: `translations\export.fr.yaml`

## Summary
This YAML file contains French translations for subscription export functionality. It defines labels for subscription-related fields (ID, email, name, frequency, shipping date) that are likely used when exporting subscription data to files or displaying it in the French locale. The translations enable the application to present subscription export information in French to users.

